<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the cache subfolder the Export screen writes CSVs into is exposed via FileProvider. -->
    <cache-path name="exports" path="exports/" />
</paths>
